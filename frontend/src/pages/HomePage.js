import React, { useContext } from "react";
import AuthContext from "../context/AuthContext";

const HomePage = () => {
    return (
        <div>
            <h1>Contacts App</h1>
            <h3>Hello</h3>
            <p>Your are logged to the home page!</p>
        </div>
    )
}

export default HomePage